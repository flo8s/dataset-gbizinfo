name: Sync

on:
  push:
    branches: [main]
    paths-ignore:
      - "*.md"
  schedule:
    - cron: "0 20 1 * *" # 毎月1日 05:00 JST
  workflow_dispatch:

concurrency:
  group: sync
  cancel-in-progress: false

jobs:
  sync:
    uses: flo8s/dataset-shared/.github/workflows/sync.yml@main
    with:
      dataset_name: gbizinfo
      s3_bucket: ${{ vars.S3_BUCKET }}
    secrets: inherit
